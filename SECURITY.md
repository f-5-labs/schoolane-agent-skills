# Security

Do not open public issues containing access tokens, school data, student data, credentials, or vulnerability details. Report security concerns privately through the repository's GitHub security advisory form or contact `support@schoolane.app`.

This repository contains agent instructions and MCP connection metadata. Authorization is enforced by the SchooLane server, not by these skills. A skill or tool annotation must never be treated as proof that an operation is allowed.
